import React from 'react'

export function SearchResultsData(query) {

    return [
        {
            title: '', //calculate title from stock shortName and symbol
            symbol: '', //raw symbol for adding to stock watchlist
        },
        
    ]
}